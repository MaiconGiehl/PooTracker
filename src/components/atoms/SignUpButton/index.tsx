import React, { Button, TextInput } from "react-native";
import loginStyles from "../../../../assets/styles/login.styles";

const SignUpButton = () => (
  <button style={loginStyles.signupButton}>Sign Up</button>
);

export default SignUpButton;
